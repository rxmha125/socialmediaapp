<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 w-full min-h-screen text-white p-10">
    <div class="flex justify-end w-full">
        <a class="rounded-md bg-red-500 px-3 py-2 text-sm mb-3 inline-block" href="/logout">Logout</a>
    </div>
    <h3 class="text-3xl"><span class="font-light">Hello</span>, <%= user.name %></h3>
    <h5 class="mb-5">Edit your post.</h5>

    <form action="/update/<%= post._id %>" method="post">
        <textarea placeholder="What's on your mind?" 
                  class="block p-3 outline-none w-full sm:w-3/4 md:w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md" 
                  name="content"><%= post.content %></textarea> <!-- Pre-fill with post content -->
        <input type="submit" value="Update post" class="w-40 block bg-yellow-500 text-white px-3 py-2 rounded-md cursor-pointer transition duration-300 hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-blue-500 mt-2">
    </form>   
</body>
</html>